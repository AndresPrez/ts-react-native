This directory ("components") must contains all of the applications components. Specially those generic ones instanced many times across the different views.
Note that any styles defined by the react native stylesheet object must be included in the components file or together with it in its own directory.
